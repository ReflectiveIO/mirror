use crate::rays::Properties;
use crate::slg::textures::fresnel::texture::FresnelTexture;

/// Fresnel preset texture
pub fn alloc_fresnel_preset_texture(props: &Properties, name: &str) -> Box<dyn FresnelTexture> {
    todo!()
}
